# 续作记录 · 2026-09-22

## 仓库位置与基线

- 远端：https://github.com/Ctrl-ttt/City-Lens
- 本地工作目录：南京insta线下竞赛/City-Lens（按各成员电脑选择磁盘位置）。
- 本次基线：`de11934`（main）；保留原有 React/FastAPI 实现并继续补齐。
- 本次续作包含以下实现、测试和交接文档；提交版本以 Git 历史为准。

## 已完成

1. 克隆仓库，安装前端与 Python 虚拟环境依赖。Git/Python 安装通过现有本地代理完成，未修改全局 Git 或系统网络配置。
2. 修复 pnpm 构建授权占位配置和空 CSS 导入；TypeScript 与 Vite 正式构建成功。
3. 修复模型返回畸形 Markdown 围栏时未受控异常，返回规范的 invalid_model_output。
4. 修复识别失败后仍保留旧结果和待播内容的问题：失败立即清除旧播报、重播入口和结果。
5. 修复视频结束后重启时内部跳转触发会话作废的问题，并等待跳转完成再抽帧。
6. 新增后端、会话/语音、浏览器集成测试；固定后端依赖版本。
7. 新增 Windows 安装、启动、测试脚本；本地 `.env` 为未配置密钥的示例配置，受 Git 忽略。
8. 补齐 README、API 契约、团队分工、访谈与人工验收模板。

## 验证证据

运行命令：`.\scripts\test.ps1 -Browser`，退出码 0。

| 验证 | 结果 |
|---|---|
| 后端 pytest | 34 通过；有 2 条依赖弃用警告，不影响当前测试 |
| 前端 Vitest | 9 通过 |
| Playwright（自动检测到 Edge） | 7 通过 |
| TypeScript + Vite build | 通过 |
| 桌面/390px 窄屏页面 | 已检查截图，无水平溢出 |
| 安装脚本（含现有代理） | 成功，未覆盖已有 .env |

浏览器测试使用**独立无头浏览器（优先系统 Edge）、模拟摄像头、本地固定样例 API 和合成 MP4**。覆盖相机抽帧、看牌、停止释放输入、输入切换丢弃迟到结果、连续三次错误自动暂停、暂停后停止抽帧、视频拖动与播放完重启、用户确认门槛及窄屏布局。测试素材不属于真实识别成绩。

系统可检测到 Microsoft Huihui 中文语音，但尚未人工确认扬声器输出、播报听感和真实环境表现。Python 实测 3.14.6，Node 24.18.0，pnpm 11.19.0。Python 3.13 和第二台机器仍待验证。

## 下一步优先级

1. B/C 在本机 `.env` 配置真实 API Key、Base URL、模型名，启动 live；先请求一张授权图片，确认模型权限与格式，再测看牌。密钥不提交或粘贴到公开材料。
2. A/B 在演示电脑检查真实相机、Link 2 朝向、中文语音、暂停和静音；样例页面不能作为真实识别证明。
3. C/D 建立有授权素材与冻结评测集，测至少 30 次真实请求和 P90，记录失败及无依据内容。
4. D 完成三位匿名访谈、核实赛事截止点、采集真实演示与制作材料。当前模板没有虚构访谈、价格、成绩或回执。
5. B 在第二台机器复现并组织 10 分钟运行和三次完整演示；之后审查本地修改再提交 PR。

暂无真实模型调用、真实用户访谈、正式录像、PPT/PDF、BOM 金额或赛事提交记录。完整人工验收表见 验收记录.md。

## 补充续作 · 无障碍改版样式表收尾

- 背景：上一轮把界面改成“当前提示优先 + 大字/高对比开关”后，在替换压缩样式表（`frontend/src/style.css`）时被中断——页面结构已更新，旧样式表已删除，新样式尚未写入，前端一度处于无法构建的临时状态。
- 完成：新增完整样式表 `frontend/src/brand.css`（设计令牌、页头/当前提示/操作区/观察窗口/识别详情/语音/运行详情、大字模式、高对比模式、1080px 与 720px 断点）；移除 `main.tsx` 中已失效的 `./style.css` 导入，`App.tsx` 统一导入 `brand.css`。
- 修复：390px 窄屏下观察窗口占位文案超出 16:9 取景框被裁切，窄屏改用 4:3 比例并缩小取景图标。
- 新增回归测试 `frontend/e2e/display-modes.spec.ts`：锁定大字模式字号变大、高对比模式白底黑字与黑底白字配色契约，以及窄屏占位文案不溢出取景框。

验证命令：`.\scripts\test.ps1 -Browser`，退出码 0。

| 验证 | 结果 |
|---|---|
| 后端 pytest | 34 通过（2 条依赖弃用警告） |
| 前端 Vitest | 9 通过 |
| Edge Playwright | 9 通过（原 7 项 + 2 项显示模式） |
| TypeScript + Vite build | 通过，CSS 产物 14.0 kB |
| 高对比配色 | 页面白底黑字、提示面板黑底白字、主操作黑底白字，均由计算样式断言确认 |
| 390px 窄屏 | 无横向溢出，占位文案完整可见 |
| 1440px 桌面 | 已检查截图，两栏布局与页头工具区正常 |

样式与显示模式的结论来自无头浏览器的计算样式与截图；真实相机、真实模型、真实语音和第二台机器仍未验证。

## 补充续作 · 连续本地跟踪与延迟结果收尾

- 普通固定视角新增浏览器端内存轨迹：发送帧保留短期灰度快照，结果返回后按纹理匹配重放实际中间帧，再以短窗口外推方向和粗略远近；匹配不唯一、场景突变、结果过期或目标消失时不修改位置，并取消无法可靠定位的旧障碍播报。
- 播报使用跟踪后的方向/接近趋势重新生成；全景仍由服务端球面方向与连续观察规则处理，不套用普通视角坐标。页面会显示原始框、当前框和短期预测框，并明确说明预测是否实际保留或取消了播报。
- 后端响应新增图像准备、模型、规则三段耗时，健康接口补充看牌模型和相机水平视场角；HTTP 抽帧节拍固定为最快每 2 秒一次，仍为 single-flight，不排队旧帧。
- 新增 `frontend/src/capture.ts`、`motion.ts` 及对应回归测试；当前可运行的前端 Vitest 为 **71 项通过**，TypeScript/Vite 生产构建通过，`git diff --check` 无空白错误。
- 本机 `.venv` 的 `pyvenv.cfg` 指向另一台电脑的 Python 路径，后端测试脚本现会明确报告该环境失效而不是伪装成测试通过。需在本机重新创建项目内 `.venv` 后再运行完整后端/浏览器测试；这一步不应删除其它目录或修改系统 Python。
