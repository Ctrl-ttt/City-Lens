# 续作记录 · 2026-09-22

## 仓库位置与基线

- 远端：https://github.com/Ctrl-ttt/City-Lens
- 本地工作目录：南京insta线下竞赛/City-Lens（按各成员电脑选择磁盘位置）。
- 本次基线：`de11934`（main）；保留原有 React/FastAPI 实现并继续补齐。
- 本次续作包含以下实现、测试和交接文档；提交版本以 Git 历史为准。

## 已完成

1. 克隆仓库，安装前端与 Python 虚拟环境依赖。Git/Python 安装通过现有本地代理完成，未修改全局 Git 或系统网络配置。
2. 修复 pnpm 构建授权占位配置和空 CSS 导入；TypeScript 与 Vite 正式构建成功。
3. 修复模型返回畸形 Markdown 围栏时未受控异常，返回规范的 invalid_model_output。
4. 修复识别失败后仍保留旧结果和待播内容的问题：失败立即清除旧播报、重播入口和结果。
5. 修复视频结束后重启时内部跳转触发会话作废的问题，并等待跳转完成再抽帧。
6. 新增后端、会话/语音、浏览器集成测试；固定后端依赖版本。
7. 新增 Windows 安装、启动、测试脚本；本地 `.env` 为未配置密钥的示例配置，受 Git 忽略。
8. 补齐 README、API 契约、团队分工、访谈与人工验收模板。

## 验证证据

运行命令：`.\scripts\test.ps1 -Browser`，退出码 0。

| 验证 | 结果 |
|---|---|
| 后端 pytest | 34 通过；有 2 条依赖弃用警告，不影响当前测试 |
| 前端 Vitest | 9 通过 |
| Chrome Playwright | 7 通过 |
| TypeScript + Vite build | 通过 |
| 桌面/390px 窄屏页面 | 已检查截图，无水平溢出 |
| 安装脚本（含现有代理） | 成功，未覆盖已有 .env |

浏览器测试使用**独立无头 Chrome、模拟摄像头、本地固定样例 API 和合成 MP4**。覆盖相机抽帧、看牌、停止释放输入、输入切换丢弃迟到结果、连续三次错误自动暂停、暂停后停止抽帧、视频拖动与播放完重启、用户确认门槛及窄屏布局。测试素材不属于真实识别成绩。

系统可检测到 Microsoft Huihui 中文语音，但尚未人工确认扬声器输出、播报听感和真实环境表现。Python 实测 3.14.6，Node 24.18.0，pnpm 11.19.0。Python 3.13 和第二台机器仍待验证。

## 下一步优先级

1. B/C 在本机 `.env` 配置真实 API Key、Base URL、模型名，启动 live；先请求一张授权图片，确认模型权限与格式，再测看牌。密钥不提交或粘贴到公开材料。
2. A/B 在演示电脑检查真实相机、Link 2 朝向、中文语音、暂停和静音；样例页面不能作为真实识别证明。
3. C/D 建立有授权素材与冻结评测集，测至少 30 次真实请求和 P90，记录失败及无依据内容。
4. D 完成三位匿名访谈、核实赛事截止点、采集真实演示与制作材料。当前模板没有虚构访谈、价格、成绩或回执。
5. B 在第二台机器复现并组织 10 分钟运行和三次完整演示；之后审查本地修改再提交 PR。

暂无真实模型调用、真实用户访谈、正式录像、PPT/PDF、BOM 金额或赛事提交记录。完整人工验收表见 VALIDATION.md。
